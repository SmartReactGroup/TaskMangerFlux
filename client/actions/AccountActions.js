import HttpClient from '../utils/httpClient'

export default {
  login: (actionContext, payload, done) => {
    console.log(payload)
    // HttpClient.post(payload.url, payload.params)
    //   .then((data) => {

    //   })
    //   .catch((error) => {

    //   })
  }
}
